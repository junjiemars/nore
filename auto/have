
# have

have=`echo $have | tr [:lower:] [:upper:]`

if [ 0 -lt ${#NM_SYMBOL_TABLE[@]} ]; then
	if [  -n "${NM_SYMBOL_TABLE[$have]}" ]; then
		have=${NM_SYMBOL_TABLE[$have]}
	fi
fi
 
cat << END >> $NM_AUTO_H

#ifndef $have
#define $have  1
#endif

END
