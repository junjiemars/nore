#### -*- mode:sh -*- vim:ft=sh
####
## No More than a C build system for clang, gcc and msvc.
## https://github.com/junjiemars/nore
####
## include
####


echo $echo_n_opt "checking for $nm_include ...$echo_c_cap"

cat << END >> $NM_AUTO_ERR

----------------------------------------
checking for $nm_include

END


nm_found=no

cat << END > $NM_AUTOTEST.c

#include <$nm_include>

int main(void) {
    return 0;
}

END


nm_test="$CC -o $NM_AUTOTEST $NM_AUTOTEST.c"

eval "$nm_test >> $NM_AUTO_ERR 2>&1"

if [ -x $NM_AUTOTEST ]; then

  nm_found=yes

  echo " found"

  have=$nm_include . ${NORE_ROOT}/auto/have_header

else

  echo " not found"

  echo "----------"  	 >> $NM_AUTO_ERR
  cat $NM_AUTOTEST.c 	 >> $NM_AUTO_ERR
  echo "----------"  	 >> $NM_AUTO_ERR
  echo $nm_test    	   >> $NM_AUTO_ERR
  echo "----------"    >> $NM_AUTO_ERR
fi

rm -rf $NM_AUTOTEST*
