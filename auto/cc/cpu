#### -*- mode:sh -*- vim:ft=sh
####
## No More than a C build system for clang, gcc and msvc.
## https://github.com/junjiemars/nore
####
## cc/cpu
####


if [ "NO" = "$NM_CPU" ]; then
   :
else
	nm_cpu_opt="${cpu_opt}$NM_CPU"
	CFLAGS=${CFLAGS:+$CFLAGS}${nm_cpu_opt:+ $nm_cpu_opt}
fi

