
# std


if [ "NO" = "$NM_STD" ]; then
  :;

elif [ "YES" = "$NM_STD" ]; then
  nm_std_opt="${std_opt}c99"

else
  nm_std_opt="${std_opt}`echo $NM_STD | tr [:upper:] [:lower:]`"

fi


if [ "NO" != "$NM_STD" ]; then
	CFLAGS=${CFLAGS:+$CFLAGS}${nm_std_opt:+ $nm_std_opt}
fi



