
# define 

if [ 0 -lt ${#NM_SYMBOL_TABLE[@]} ]; then
	if [ -n "${NM_SYMBOL_TABLE[$have]}" ]; then
		have=${NM_SYMBOL_TABLE[$have]}
		echo "!!! have=$have"
	fi
fi

cat << END >> $NM_AUTO_H

#ifndef $have
#define $have  $value
#endif

END
