language: bash

matrix:
  include:
    - os: linux
    - os: osx

env:
  - SHELL=bash
  # - NORE_TEST_DIR=$TRAVIS_BUILD_DIR/nore_test

before_script:
  - mkdir -p test
  - cd test
  - bash <(curl https://raw.githubusercontent.com/junjiemars/nore/edge/bootstrap.sh)

script: 
  - $TRAVIS_BUILD_DIR/.travis-tests.sh
